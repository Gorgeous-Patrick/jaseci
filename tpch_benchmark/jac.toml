[project]
name = "tpch_benchmark"
version = "0.1.0"
description = "TPC-H benchmark for Jac — graph construction and 22 query walkers"
entry-point = "main.jac"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
