- **Fix: Remove redundant commits in server walker path**: `ScaleTieredMemory.close()` no longer commits -- callers are responsible for persisting before close. This eliminates redundant hash-scans of the entire L1 cache on every request.
