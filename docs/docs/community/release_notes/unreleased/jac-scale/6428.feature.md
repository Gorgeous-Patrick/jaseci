- **Feature: Yappi profiler admin endpoints**: Add `/admin/profiler/start`, `/admin/profiler/stop`, `/admin/profiler/status` for on-demand CPU/wall profiling of the running server. Stop saves `.prof` (pstat-compatible) and `.txt` (human-readable) files.
